#include "push_swap.h"

int	swap(t_value *stack, int size)
{
	int	value;
	int	index;

	if (size >= 2)
	{
		value = stack->value;
		index = stack
	}
}

int	ps(const char *op, int *a, int *b)
{
	if (ft_strncmp(op, "sa", 2) == 0)
	{
		
	}
}